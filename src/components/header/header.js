import React from 'react';
import './header.scss';

import Logo from '../logo/logo'

const Header = (props) => {
  return (
      <header>
          <Logo />

    </header>
  )
}

export default Header;
